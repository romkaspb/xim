<%= form_for(status_log) do |f| %>
  <% if status_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status_log.errors.count, "error") %> prohibited this status_log from being saved:</h2>

      <ul>
      <% status_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %>
    <%= f.number_field :status %>
  </div>

  <div class="field">
    <%= f.label :plane_id %>
    <%= f.text_field :plane_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
